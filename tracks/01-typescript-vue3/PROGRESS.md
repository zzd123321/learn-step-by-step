# TypeScript + Vue 3 + Node.js 学习进度

## 当前 lesson

- L010：泛型函数入门

## 已讲授状态

- L001：已讲授
- L002：已讲授
- L003：已讲授
- L004：已讲授
- L005：已讲授
- L006：已讲授
- L007：已讲授
- L008：已讲授
- L009：已讲授
- L010：已讲授

## 小练习状态

- L001：待完成
- L002：待完成
- L003：待完成
- L004：待完成
- L005：待完成
- L006：待完成
- L007：待完成
- L008：待完成
- L009：待完成
- L010：待完成

## 已验证命令

在 `tracks/01-typescript-vue3/examples/L001-why-typescript/` 下已真实执行：

- `node src/runtime-error.js`
- `tsc --noEmit`
- `tsc`
- `node dist/typed-solution.js`

在 `tracks/01-typescript-vue3/examples/L002-compiler-flow/` 下已真实执行：

- `tsc --noEmit`
- `tsc --listEmittedFiles --pretty false`
- `node dist/index.js`
- `sed -n '1,120p' dist/index.js`

在 `tracks/01-typescript-vue3/examples/L003-annotations-inference/` 下已真实执行：

- `tsc --noEmit`
- `tsc`
- `node dist/index.js`
- `sed -n '1,160p' dist/index.js`

在 `tracks/01-typescript-vue3/examples/L004-basic-object-function-types/` 下已真实执行：

- `tsc --noEmit`
- `tsc`
- `node dist/index.js`
- `sed -n '1,180p' dist/index.js`

在 `tracks/01-typescript-vue3/examples/L005-arrays-tuples-literals/` 下已真实执行：

- `tsc --noEmit`
- `tsc`
- `node dist/index.js`
- `sed -n '1,180p' dist/index.js`

在 `tracks/01-typescript-vue3/examples/L006-interface-and-type/` 下已真实执行：

- `tsc --noEmit`
- `tsc`
- `node dist/index.js`
- `sed -n '1,180p' dist/index.js`

在 `tracks/01-typescript-vue3/examples/L007-unions-intersections-properties/` 下已真实执行：

- `tsc --noEmit`
- `tsc`
- `node dist/index.js`
- `sed -n '1,220p' dist/index.js`

在 `tracks/01-typescript-vue3/examples/L008-narrowing-type-guards/` 下已真实执行：

- `tsc --noEmit`
- `tsc`
- `node dist/index.js`
- `sed -n '1,240p' dist/index.js`

在 `tracks/01-typescript-vue3/examples/L009-never-unknown-any/` 下已真实执行：

- `tsc --noEmit`
- `tsc`
- `node dist/index.js`
- `sed -n '1,260p' dist/index.js`

在 `tracks/01-typescript-vue3/examples/L010-generic-functions/` 下已真实执行：

- `tsc --noEmit`
- `tsc`
- `node dist/index.js`
- `sed -n '1,220p' dist/index.js`

## 下一节建议

- L011：泛型约束
