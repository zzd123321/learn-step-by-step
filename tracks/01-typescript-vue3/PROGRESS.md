# TypeScript + Vue 3 + Node.js 学习进度

## 当前 lesson

- L003：类型标注与类型推断

## 已讲授状态

- L001：已讲授
- L002：已讲授
- L003：已讲授

## 小练习状态

- L001：待完成
- L002：待完成
- L003：待完成

## 已验证命令

在 `tracks/01-typescript-vue3/examples/L001-why-typescript/` 下已真实执行：

- `node src/runtime-error.js`
- `tsc --noEmit`
- `tsc`
- `node dist/typed-solution.js`

在 `tracks/01-typescript-vue3/examples/L002-compiler-flow/` 下已真实执行：

- `tsc --noEmit`
- `tsc --listEmittedFiles --pretty false`
- `node dist/index.js`
- `sed -n '1,120p' dist/index.js`

在 `tracks/01-typescript-vue3/examples/L003-annotations-inference/` 下已真实执行：

- `tsc --noEmit`
- `tsc`
- `node dist/index.js`
- `sed -n '1,160p' dist/index.js`

## 下一节建议

- L004：基础类型、对象类型与函数类型
