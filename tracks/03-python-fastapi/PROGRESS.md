# Python / FastAPI 学习进度

## 当前 lesson 编号

- L001

## 已讲授内容

- L001：Python 的执行模型与开发环境：它和 JavaScript 有哪些关键不同
  - 理解 `python3 文件路径.py` 的执行方式。
  - 理解 `__name__ == "__main__"` 的入口保护作用。
  - 初步区分入口文件和业务模块。
  - 已真实运行示例：`python3 tracks/03-python-fastapi/examples/L001-python-execution-model/main.py Ada`

## 待练习内容

- L001 小练习：在 `user_service.py` 中新增 `build_api_path(resource: str) -> str`，并在 `main.py` 中打印 `/api/v1/users`。

## 当前状态

- L001 已讲授，待完成小练习。
