# 数据库 / 网络学习进度

## 当前 lesson 编号

- L001

## 已讲授内容

- L001：浏览器一次请求到服务端数据库之间到底发生了什么
  - 建立了从浏览器、HTTP、服务端路由、参数校验、SQL 写入到数据库响应的完整链路图。
  - 使用 Python 标准库 HTTPServer 和 SQLite 创建最小可运行示例。
  - 通过 `curl` 验证健康检查、POST 写入和 GET 查询。

## 待练习内容

- 修改 POST 请求中的 `source` 字段并重新查询。
- 故意删除 `action` 字段，观察 422 响应。
- 在 `payload` 中增加 `userId`，观察数据库保存的 JSON。
- 使用 `PORT=8014` 改变服务监听端口。

## 当前状态

- 已完成 L001，下一节进入关系型数据库的表、行、列、主键、外键与约束。
