# 本节默认使用本地 mock provider，不需要真实 API Key。
AI_PROVIDER=mock
AI_MODEL=mock-stable-v1
AI_MAX_INPUT_CHARS=200

# 未来接入真实模型 API 时，只在本地 .env 中填写真实密钥。
# 不要把 .env 或真实 Token 提交到 Git。
# OPENAI_API_KEY=replace_with_your_local_key
